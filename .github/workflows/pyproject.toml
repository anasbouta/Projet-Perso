[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ATM-SQL-TEST"
version = "0.1.0"
authors = [
  { name="Anas Boutaghroucht" },
]
description = "Un système ATM sécurisé avec interface Tkinter et SQLite"
dependencies = [
    "bcrypt",
]
